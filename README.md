# Draw.io MCP Server

MCP сервер для роботи з Draw.io діаграмами в Kiro CLI. Парсить схеми, витягує компоненти, класи, зв'язки та текстовий вміст.

## Можливості

### 📊 get_diagram_overview
Швидкий огляд діаграми:
- Кількість сторінок
- Загальна кількість елементів
- Список всіх сторінок
- Статистика компонентів та з'єднань

### 📦 parse_drawio
Детальний парсинг діаграми:
- Витягує всі компоненти
- Визначає типи елементів (процеси, рішення, бази даних, хмари)
- Підтримує фільтрацію по сторінках
- Показує статистику по типах

### 📝 extract_text_content
Витягує весь текст з діаграми:
- Всі написи та мітки
- Описи та нотатки
- Групує по сторінках
- Очищає від HTML тегів

### 🏛️ extract_classes
Витягує класи з діаграм класів:
- Назви класів
- Методи (з дужками)
- Поля (без дужок)
- Інформація про сторінку

### 🔗 extract_relationships
Аналізує зв'язки між компонентами:
- Наслідування (inheritance)
- Композиція (composition)
- Агрегація (aggregation)
- Залежності (dependency)
- Асоціації (association)

## Встановлення

Сервер встановлено глобально в `~/.kiro-mcp-servers/drawio/`

Конфігурація: `~/.config/kiro-cli/mcp-config.json`

```json
{
  "mcpServers": {
    "drawio": {
      "command": "node",
      "args": ["~/.kiro-mcp-servers/drawio/index.js"],
      "env": {}
    }
  }
}
```

## Використання

### Базові команди

```
Дай огляд діаграми docs/architecture.drawio
Прочитай схему diagram.xml
Витягни текст з drawio/api-flow.drawio.xml
Покажи класи з class-diagram.drawio
Які зв'язки в схемі?
```

### Приклади використання

**Аналіз архітектури:**
```
Проаналізуй архітектуру з файлу docs/system-design.drawio
```

**Порівняння з кодом:**
```
Порівняй класи з діаграми class-diagram.xml з Java класами в src/
```

**Генерація документації:**
```
На основі API схеми створи документацію endpoints
```

**Валідація:**
```
Перевір чи всі компоненти з діаграми реалізовані в коді
```

**Витягування бізнес-логіки:**
```
Витягни всі процеси та рішення з бізнес-процесу
```

## Підтримувані формати

- `.drawio` - нативний формат Draw.io
- `.xml` - XML експорт з Draw.io
- `.drawio.xml` - Draw.io XML

## Підтримувані типи елементів

- **swimlane** - контейнери, класи
- **process** - процеси, дії
- **decision** - рішення (ромби)
- **database** - бази даних (циліндри)
- **cloud** - хмарні сервіси
- **document** - документи
- **rectangle** - прямокутники
- **ellipse** - еліпси
- **rounded** - закруглені форми

## Типи зв'язків

- **inheritance** - наслідування (стрілка з блоком)
- **composition** - композиція (ромб)
- **aggregation** - агрегація (порожній ромб)
- **dependency** - залежність (пунктир)
- **association** - асоціація (звичайна лінія)

## Оновлення

```bash
cd ~/.kiro-mcp-servers/drawio
npm update
```

## Видалення

```bash
rm -rf ~/.kiro-mcp-servers/drawio
# Видалити секцію "drawio" з ~/.config/kiro-cli/mcp-config.json
```

## Версія

2.0.0 - Покращена обробка, форматування виводу, підтримка багатосторінкових діаграм
